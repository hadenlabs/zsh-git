# Docker Compose
# https://docs.docker.com/compose/

version: "3"
services:
  # Python
  app:
    environment: &environment
      - STAGE=local
    image: zsh-git-aliases-app-dev:0.0.3
    build:
      args:
        stage: dev
        version: 0.0.3

  check:
    image: zsh-git-aliases-check-dev:0.0.3
    environment: *environment
    build:
      context: .
      dockerfile: provision/docker/check/Dockerfile
      args:
        stage: dev
        version: 0.0.3
    volumes:
      - .:/usr/src
    env_file:
      - .env
